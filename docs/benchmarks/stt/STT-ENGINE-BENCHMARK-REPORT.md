# STT Engine Benchmark Report

**WO:** WO-040 — Real Acoustic STT Benchmark & Engine Selection Evidence Gate
**Corrective:** WO-040-CORR (measurements) and WO-040-CORR-02 (genuine COLD/WARM lifecycle)
**Date:** 2026-09-02
**Baseline / parent:** `bcdad704644c80f5b2a21f5342e591cf6f04f782` (the ADR-014 commit)
**Corrective baseline:** `26a3179a097992d99a9dbb865631bebd089ba146` (WO-040-CORR)
**ADR:** [ADR-014](../adr/ADR-014-Real-Acoustic-STT-Engine-Selection-and-Benchmark-Gate.md) (Status: Proposed)
**Current production STT decision:** `NOT_YET_JUSTIFIED`

> **Summary outcome:** The benchmark was **NOT executed**. No production STT
> engine is authorized. The engine-selection gate established by ADR-014 is
> **NOT satisfied**. This report documents the actual available data and the
> precise reasons the benchmark could not be run on this host. No result was
> fabricated; every aggregate below is either a real measurement or an explicit
> `NOT EXECUTED` with its reason.

---

## Tooling status vs. benchmark execution status

It is important to distinguish two different things:

**TOOLING STATUS** — the benchmark harness is implemented and validated. It
is capable of executing the ADR-014 benchmark *when* a candidate runtime/model
and a dataset of real radio recordings are provisioned locally. The harness
runs each candidate through a genuine COLD/WARM session lifecycle (the
candidate is initialized exactly once and reused for warm inference) and
measures latency, RTF, CPU, RAM, GPU, VRAM, WER, CER, callsign accuracy, and
accounts for failures and timeouts per record. This status is **VALIDATED**
(`docs/benchmarks/stt/test_run_benchmark.py` passes).

**BENCHMARK EXECUTION STATUS** — the benchmark has **NOT** been executed,
because the prerequisites are absent on this host (see §8/§9 and §25). This
status is **NOT EXECUTED**.

```text
Benchmark execution status: NOT EXECUTED
ADR-014 gate:               NOT SATISFIED
```

The fact that the tooling is correct must not be read as the benchmark being
run, and it must not be read as the ADR-014 gate being satisfied. The two are
independent.

---

## 1. Objective

Execute a reproducible real-world benchmark of the recognised STT candidates
(`faster_whisper`, `vosk`) on representative real radio audio from the actual
TACTICAL_CORE audio pipeline, to create objective evidence for a later
architecture decision. This WO does **not** select or authorize a production
engine.

## 2. Dataset description

The only WAV masters available on this host are the recordings the actual
project pipeline (RTP → VAD → WAV master → STT seam) wrote to the archive root.
On this host they are located under:

```text
/tmp/tmp830mejt0/2026/09/02/radio/
/tmp/hv_nw8kok3b/2026/09/02/radio/
/tmp/aud_a03080ty/2026/09/02/radio/
```

Provenance (established by inspecting `backend/app/audio/rtp_simulator.py` and
`backend/tests/test_wo039b_recording.py`): these are **WO-039-B/C unit-test
fixtures**. They were produced by feeding a constant-amplitude PCM value (e.g.
`_pcm(20000)`, a "speech tone" at RMS ~20000 over a silence floor at ~50)
through the real pipeline to exercise the VAD / segmentation / recording logic.

Consequence: they contain **no speech, no words, no callsigns**. Two of the
recordings (`64b8be8b8f77`, `1a9bfad300ef`) have exactly **2 distinct sample
values** — a constant 20000-value tone over a 50-value silence floor. The
remaining recordings have ~250 distinct values, which is the A-law companding
quantization range for a constant/full-scale test value. None of the 36 is a
real radio transmission with linguistic content.

## 3. Dataset size

```text
Total WAV masters found:     36
Format-compliant (mono/16-bit/8 kHz): 36 (all)
Distinct SHA-256:            36 (all unique)
Real radio transmissions:    0  (all are test fixtures)
```

The mandatory minimum is **50 independently verified real radio transmissions**
(ADR-014). Only **36** masters exist, and **0** of them are real radio
transmissions. The requirement is therefore **not met** by a wide margin, and
the shortfall is **not** redefined or silently waived.

## 4. Ground-truth methodology

WO-040 §5 requires a manually verified reference transcript for every
transmission, with `audio_id / source / timestamp / duration /
reference_transcript / callsigns_present`, never generated by a candidate
engine. **No ground-truth transcripts exist on this host.** There is no
manually verified reference for any recording, and none can be derived honestly:
the recordings contain no words or callsigns to transcribe. `dataset_manifest.csv`
records `ground_truth=""` for every row. A deterministic WAV → reference
transcript mapping cannot be established.

## 5. Audio normalization

All 36 masters are project-native: mono, 16-bit PCM, 8 kHz (this is exactly the
format the WO-039-C3 worker validates and the format produced by the G.711
A-law decode path). Since no engine inference ran, no per-candidate
normalization was applied. A normalization step, if the benchmark were
executable, would resample everything to a single documented format; this is
moot here because no engine ran.

## 6. Hardware

```text
CPU:  Intel(R) Xeon(R) Platinum 8568Y+, 8 cores (1 socket, 1 thread/core)
RAM:  15997 MB (~16 GiB)
GPU:  none present (no nvidia-smi; no VGA/3D device in lspci)
```

## 7. OS / runtime

```text
OS:          Debian GNU/Linux 13 (trixie), kernel 6.12.48+deb13-cloud-amd64, x86_64
Python:      3.13.5
Repo venv:   /opt/data/tactical_core_github/.venv (Python 3.13.5)
```

## 8. Candidate versions

Neither candidate runtime is installed on this host:

```text
faster_whisper:  NOT INSTALLED  (ModuleNotFoundError: No module named 'faster_whisper')
vosk:            NOT INSTALLED  (ModuleNotFoundError: No module named 'vosk')
```

Related runtimes are also absent: `torch`, `ctranslate2`, `soundfile`, `numpy`
(in the repo venv), `jiwer`, `editdistance`. The harness `probe` command reports
the same (see `run_benchmark.py probe`).

## 9. Model variants

**No STT model exists on disk.** A filesystem search found no whisper or vosk
model directory, no `*.bin` model files, and no `*.gguf` files anywhere on the
host. There is therefore no model variant to record.

## 10. Candidate configuration

Not applicable — no candidate was instantiated. The repository configuration
(`backend/app/audio/stt_config.py`) recognises `SUPPORTED_ENGINES =
{faster_whisper, vosk}` as candidates only; no adapter is registered and
`build_transcriber` raises `SttEngineUnavailableError` for a recognised engine
with no registered adapter. This was not changed by WO-040.

## 11. Cold-start results

```text
NOT EXECUTED
```

Reason: no candidate runtime/model is present locally. Executing a cold-start
run would require downloading and installing the runtime and model, which is
explicitly prohibited by WO-040 §8 (offline requirement; all models and runtime
components must already exist locally). Downloading would also violate
ADR-014's engine-neutral gate.

The harness (WO-040-CORR-02) now implements a genuine COLD phase as part of a
candidate session lifecycle: the session is created and the runtime/model is
initialized exactly once, then the first record is transcribed, and cold
latency = initialization time + inference time. This is harness capability
only; because no candidate runtime/model exists, the COLD phase was not
executed.

## 12. Warm / inference results

```text
NOT EXECUTED
```

Same reason as §11.

The harness (WO-040-CORR-02) now implements a genuine WARM phase that reuses
the already-initialized candidate session for inference only, without
reconstructing the model; warm latency = inference time. This is harness
capability only; because no candidate runtime/model exists, the WARM phase was
not executed. The two phases are behaviorally distinct and recorded separately
in the per-record result model (`run_phase`), but neither produced evidence here.

## 13. WER

```text
NOT MEASURABLE
```

Reason: WER is defined against a manually verified reference transcript (§4);
none exists. Computing WER requires a candidate hypothesis, which does not exist
(§8/§9). No WER value is reported because none was measured.

## 14. CER

```text
NOT MEASURABLE
```

Same reason as §13.

## 15. Callsign accuracy

```text
NOT MEASURABLE
```

Reason: no ground-truth callsigns exist and no candidate produced a hypothesis.
The metric is defined in the harness (`run_benchmark.py callsign_accuracy`):
a callsign is correct only if it appears verbatim (word-boundary) in the
transcript. This definition is recorded for a future run but was not exercised
against real data.

## 16. End-to-end latency

```text
NOT MEASURED
```

No inference was run (§11).

## 17. RTF

```text
NOT MEASURED
```

Definition recorded for future use: `RTF = processing time / audio duration`.
No inference was run.

## 18. CPU

```text
NOT MEASURED
```

## 19. RAM

```text
NOT MEASURED
```

## 20. GPU

```text
NOT MEASURED  (and N/A — no GPU present on this host)
```

## 21. VRAM

```text
NOT MEASURED  (and N/A — no GPU present on this host)
```

## 22. Failures

```text
0 engine failures recorded
```

The benchmark was not executed, so there are no engine failures to account. The
harness and its validation tests pass (see §Testing). No failed transcription
was silently dropped because no transcription was attempted.

## 23. Timeouts

```text
0 timeouts recorded
```

Same as §22.

## 24. Comparative analysis

```text
NOT POSSIBLE — no candidate produced output.
```

A comparison of `faster_whisper` versus `vosk` cannot be made because neither
engine could be run offline on this host, and the only available audio contains
no linguistic content. No comparison is asserted.

## 25. Limitations

The benchmark could not be executed. The limitations, each independently
sufficient to block a valid benchmark:

1. **Offline candidate runtime/model absent (WO-040 §8).** Neither `faster_whisper`
   nor `vosk` is installed, and no STT model exists on disk. The WO requires all
   models and runtime components to already exist locally and prohibits
   downloading them for the benchmark. This is the documented technical
   inability to execute the benchmark for either candidate.
2. **No ground truth (WO-040 §5).** No manually verified reference transcript
   exists for any recording.
3. **Dataset is not real radio (ADR-014; WO-040 §2/§4).** All 36 available WAV
   masters are WO-039 unit-test fixtures (constant-amplitude test tones), not
   real radio transmissions. ADR-014 states that synthetic speech alone is not
   acceptable.
4. **Dataset size below the mandatory minimum.** 36 < 50 required transmissions,
   and 0 of the 36 are real radio.

## 26. Recommendation

Do **not** select either candidate. Do **not** authorize a production STT
engine. The engine-selection gate is not satisfied. Before any selection can be
considered, the following must be true:

- a candidate runtime and model are provisioned locally (offline);
- a dataset of **≥50 real radio transmissions** produced by the actual project
  pipeline is collected, each with a manually verified reference transcript and
  callsigns;
- the benchmark is run identically for both candidates (same dataset, language,
  normalized audio, hardware), recording cold-start and warm inference
  separately;
- all metrics (WER, CER, callsign accuracy, latency, RTF, CPU/RAM/GPU/VRAM,
  failures, timeouts) are measured and recorded;
- the mandatory gate is assessed and, if passed, the engine-selection decision
  is accepted by the Chief Systems Architect.

## 27. Gate result

```text
Benchmark executed:            NO
Benchmark report recorded:     YES (this document, recording the limitation)
Mandatory gate passed:         NO — not assessed (no benchmark evidence)
Production engine authorized:  NO
Engine selection finalized:    NO
ADR-014 status:                Proposed
Decision:                      NOT_YET_JUSTIFIED  (unchanged)
```

Per WO-040 §17/§18 and ADR-014, because the benchmark could not be executed and
the gate was not passed, the outcome is:

```text
NO PRODUCTION STT ENGINE AUTHORIZED
```

---

## Appendix — Verification evidence

- STT regression suite (`backend/tests/test_wo039c_stt.py`): **47 passed**.
- Benchmark-specific tests (`docs/benchmarks/stt/test_run_benchmark.py`): **41 passed**.
- `dataset_manifest.csv`: **36 rows**, 36 distinct SHA-256, all format-compliant,
  `ground_truth=""` for all, `real_transmission="false"` for all (the fixtures
  are not relabelled as real radio).
- Candidate probe: both candidates `runtime_installed=false`, `model_present=false`.
- Benchmark harness (WO-040-CORR): the per-record result model, candidate runner
  boundary, latency/RTF/CPU/RAM/GPU/VRAM measurement, failure/timeout accounting,
  aggregation, cold/warm support, results CSV (LF), and ADR-014 gate evaluation
  are implemented and validated by the 41 benchmark-specific tests.
- Benchmark harness (WO-040-CORR-02): the COLD/WARM phases are now genuine and
  behaviorally distinct. A candidate session is created and initialized exactly
  once (COLD), and the same initialized session is reused for every warm
  inference (WARM); the model is never reconstructed per warm input. Cold
  latency = initialization + inference; warm latency = inference only. This is
  proven by lifecycle tests (`test_cold_warm_lifecycle_initializes_once` and
  others) using deterministic fake sessions — fakes are never treated as real
  benchmark evidence.
- No production source, test, backlog, schema, or existing-ADR file was modified
  (see the WO-040-CORR / WO-040-CORR-02 mutation proof in the work-order report).
